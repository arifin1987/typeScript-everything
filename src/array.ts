const myArray:string[] = ['kamal', 'jamal', 'salam', 'rabbi'];


const rollNums:number[] = [12, 14, 15, 16];

// A tuple is a typed array with a pre-defined length and types for each index.
const mixedArray:[string,number, number,boolean] = ["Samsu", 120, 125/5, false];

